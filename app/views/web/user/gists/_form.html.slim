= simple_form_for [:user, gist] do |f|
  = f.input :description
  = f.input :content, input_html: { class: 'large-text-input' }
  = f.input :public, as: :boolean
  = f.input :language, as: :select, collection: gist_languages_collection
  = f.button :submit, class: 'btn btn-success'
